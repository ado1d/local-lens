<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Post - LocalLens</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<%- include('../partials/header') %>
<div class="container my-5" style="max-width: 800px;">
  <h2 class="mb-4 text-center text-danger">Activity Log</h2>

  <% if (posts.length === 0) { %>
    <p class="text-center">No recent activity in your area.</p>
  <% } else { %>
    <div class="list-group">
      <% posts.forEach(post => { %>
        <div class="list-group-item mb-3">
          <h5><%= post.title %></h5>
          <p class="mb-1"><%= post.content %></p>
          <small class="text-black">Posted on <%= new Date(post.created_at).toLocaleString() %></small>
        </div>
      <% }) %>
    </div>
  <% } %>
</div>
<%- include('../partials/footer') %>
</body>
</html>
